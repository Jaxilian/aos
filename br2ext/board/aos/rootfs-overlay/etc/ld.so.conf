# glibc's loader reads /etc/ld.so.cache, which ldconfig builds from this.
# The image has no ld.so.conf of its own; the only addition to the default
# search path is what the package manager exposes.
include /etc/ld.so.conf.d/*.conf
