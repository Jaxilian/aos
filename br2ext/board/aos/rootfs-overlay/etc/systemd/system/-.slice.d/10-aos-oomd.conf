# Let systemd-oomd act on the whole system when swap is nearly full (its
# SwapUsedLimit, 90% by default). Without a ManagedOOM* setting on some
# slice, oomd is running but has nothing to manage.
[Slice]
ManagedOOMSwap=kill
