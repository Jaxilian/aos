# Kill the worst offender in a user's session when that session has been
# under sustained memory pressure, rather than letting the whole machine
# thrash. The kernel OOM killer only acts when memory is gone; this acts on
# pressure, before that. Same values Fedora ships.
[Service]
ManagedOOMMemoryPressure=kill
ManagedOOMMemoryPressureLimit=50%
