# Whose session the desktop is. ade.service itself names an "ade" service
# account; on AOS the session belongs to the person using the machine, so
# the terminal it opens is their shell in their home and sudo asks their
# password. The image ships with the demo account here; aos-install
# --release rewrites this file with the account it creates.
[Service]
User=admin
