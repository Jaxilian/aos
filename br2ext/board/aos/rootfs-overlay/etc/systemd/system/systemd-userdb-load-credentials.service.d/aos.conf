# Loads user records handed in as system credentials into /etc/userdb. On
# the read-only live ISO there is nowhere to put them and the unit fails;
# it is only meaningful where /etc can be written.
[Unit]
ConditionPathIsReadWrite=/etc
