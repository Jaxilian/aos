# Pet the hardware watchdog every 30s; if systemd itself stops, the machine
# reboots instead of hanging. RebootWatchdogSec covers a reboot that wedges
# on the way down. Both are no-ops on hardware with no watchdog device.
[Manager]
RuntimeWatchdogSec=30s
RebootWatchdogSec=10min
