# For systemd user managers, should one ever run on AOS. The desktop is a
# system unit today and gets these from ade.service.d/20-apm.conf.
PATH=/opt/apm/bin:${PATH}
XDG_DATA_DIRS=/opt/apm/exports/share:${XDG_DATA_DIRS:-/usr/local/share:/usr/share}
