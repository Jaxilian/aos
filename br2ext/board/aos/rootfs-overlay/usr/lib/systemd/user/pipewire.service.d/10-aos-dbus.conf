# Start after the session bus is listening. All three are pulled in with
# the session and race dbus.socket otherwise; PipeWire then tries X11
# autolaunch for a bus it could not find, logs it, and its portal and
# D-Bus features are dead until the next restart.
[Unit]
After=dbus.socket
Wants=dbus.socket
